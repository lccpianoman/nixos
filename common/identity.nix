{
  # Git identity shared across hosts. nixnotdix consumes this via the
  # home-manager git module; nixvps via the NixOS programs.git module.
  name = "Luke Collins";
  email = "luke@collins.rocks";
}
